#include <iostream>
#include "board.h"


int main(int args, char *argv[]) {

    for (int i = 1; i < args; i++) {
        string cmd = argv[i];
        if (cmd == "-text"){

        }
        if (cmd == "-enablebonus"){

        }
        if (cmd  == "-seed"){

        }
        if (cmd == "-scriptfile1"){

        }
        if (cmd == "-scriptfile2"){

        }
        if (cmd == "-startlevel"){

        }
    }
}